import numpy as np
def preprocess_data(data):
    return data

def train_model(X_train,y_train,X_test,y_test):
    accuracy=np.random.randint(0,1)
    return accuracy

def test_model(X_train,y_train,X_test,y_test):
    accuracy=np.random.randint(0,1)
    return accuracy